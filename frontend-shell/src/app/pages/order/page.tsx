'use client'
import { Popover, TextInput, Title } from '@mantine/core';

// This will throw an error
export default function Page() {
    return (
        <>
            <Title>This is order page</Title>
        </>
    );
}
