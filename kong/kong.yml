_format_version: "3.0"

plugins:
  - name: cors
    config:
      origins:
        - http://localhost:3000
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Authorization
        - Content-Type
      credentials: true

services:
  - name: orders-service
    url: http://backend-orders:3001
    routes:
      - name: orders-route
        paths:
          - /api
        strip_path: true
